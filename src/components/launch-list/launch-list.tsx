import React from "react";
import { useQuery, gql } from '@apollo/client';
// import launch-

const LaunchList = () => {

  return (
    <div id="detail-view">

    </div>
  )
}

export default LaunchList;